<Window x:Class="WpfApp1.NotificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        Width="360" Height="90"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Focusable="False">

    <Window.Resources>
        <Storyboard x:Key="SlideInToast">
            <DoubleAnimation Storyboard.TargetName="ToastCard"
                             Storyboard.TargetProperty="Opacity"
                             From="0" To="1" Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <ThicknessAnimation Storyboard.TargetName="ToastCard"
                                Storyboard.TargetProperty="Margin"
                                From="0,0,0,-20" To="6,6,6,6"
                                Duration="0:0:0.35">
                <ThicknessAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </ThicknessAnimation.EasingFunction>
            </ThicknessAnimation>
        </Storyboard>
    </Window.Resources>

    <Grid>
        <!-- Glow backdrop -->
        <Border CornerRadius="14" Margin="4">
            <Border.Effect>
                <DropShadowEffect Color="#7C3AED" BlurRadius="20" ShadowDepth="0" Opacity="0.45"/>
            </Border.Effect>
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#EE0A0A1A" Offset="0"/>
                    <GradientStop Color="#EE1A1035" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
        </Border>

        <!-- Glass toast card -->
        <Border x:Name="ToastCard" CornerRadius="14" Margin="6,6,6,6" Opacity="0">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#D80F0F20" Offset="0"/>
                    <GradientStop Color="#D81A1035" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Border.BorderBrush>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#50FFFFFF" Offset="0"/>
                    <GradientStop Color="#15FFFFFF" Offset="0.5"/>
                    <GradientStop Color="#35A855F7" Offset="1"/>
                </LinearGradientBrush>
            </Border.BorderBrush>
            <Border.BorderThickness>1</Border.BorderThickness>

            <Grid Margin="14,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Icon with glow -->
                <Border Grid.Column="0"
                        Width="42" Height="42" CornerRadius="21"
                        VerticalAlignment="Center" Margin="0,0,12,0">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#CC7C3AED" Offset="0"/>
                            <GradientStop Color="#CC5B21B6" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <Border.Effect>
                        <DropShadowEffect Color="#A855F7" BlurRadius="14" ShadowDepth="0" Opacity="0.8"/>
                    </Border.Effect>
                    <TextBlock x:Name="lblIcon" Text="!"
                               Foreground="White" FontSize="20" FontWeight="Bold"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <!-- Text -->
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock x:Name="lblTitle" Text="Notification"
                               Foreground="White" FontSize="13" FontWeight="SemiBold"
                               TextTrimming="CharacterEllipsis"/>
                    <TextBlock x:Name="lblMessage" Text="You have a new message"
                               Foreground="#AAFFFFFF" FontSize="11"
                               TextTrimming="CharacterEllipsis" Margin="0,3,0,0"/>
                </StackPanel>

                <!-- Close -->
                <Button Grid.Column="2" Content="✕"
                        Width="22" Height="22" FontSize="10"
                        Foreground="#60FFFFFF" Background="Transparent"
                        BorderThickness="0" Cursor="Hand"
                        VerticalAlignment="Top"
                        Click="BtnDismiss_Click"/>
            </Grid>
        </Border>
    </Grid>

    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard Storyboard="{StaticResource SlideInToast}"/>
        </EventTrigger>
    </Window.Triggers>
</Window>
